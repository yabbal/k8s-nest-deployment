apiVersion: v1
kind: Service
metadata:
  name: nest-app-service
  namespace: nest-app
spec:
  selector:
    app: nest-app-preview
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
